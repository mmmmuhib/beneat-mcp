# Railway auto-sets PORT
# PORT=3001

# Solana RPC
SOLANA_RPC_URL=https://api.devnet.solana.com
HELIUS_RPC_URL=
HELIUS_API_KEY=

# Auth — set to enable bearer token auth (omit for open access)
MCP_AUTH_TOKEN=

# SOL price for USD calculations
SOL_PRICE_USD=150

# Public URL for OAuth discovery (auto-detected from Host header if not set)
# PUBLIC_URL=https://your-app.up.railway.app
